server:
  port: 8080

spring:
  application:
    name: SportsCenter

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://sportscenterdb-do-user-24951870-0.g.db.ondigitalocean.com:25060/sportscenter?useSSL=true&serverTimezone=UTC&allowPublicKeyRetrieval=true}
    username: ${SPRING_DATASOURCE_USERNAME:doadmin}
    password: ${SPRING_DATASOURCE_PASSWORD:}

  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

  data:
    redis:
      host: ${SPRING_REDIS_HOST:localhost}
      port: ${SPRING_REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      ssl:
        enabled: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: complaints.sportscenter@gmail.com
    password: fzdgzkqneuyedslk
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: 2f1d88b1c0b9dcb71c5f2d5be89801906fa2e4d84a3b19b47d3de4e154deeb71
  expiration: 86400000

stripe:
  api:
    key: ${STRIPE_SECRET_KEY}

recommendation:
  service:
    url: http://127.0.0.1:8000/recommendations


